spring:
  datasource:
    url: jdbc:postgresql://localhost:25432/postgres
    username: postgres-two
    password: postgres-two
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
